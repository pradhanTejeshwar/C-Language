#include<stdio.h>
#include<conio.h>
main ()
{
	int a,b,c,d;
	printf("Enter any three numbers");
	scanf("%d",&a);
			if((( a*a*a ) + ( b*b*b ) + ( c*c*c ))== abc )
				printf("Armstrong");
			else
				printf("Not Armstrong");
}
