#include<stdio.h>
void main()
{
 int a[10],i,n,l;

 printf("\nEnter the length of the Array : ");
 scanf("%d",&n);
 printf("\nEnter the Elements in the Array : ");
  for(i=1;i<=n;i++)
   {
    scanf("%d",&a[i]);
   }
 printf("\nElements in the Array before Insertion : ");
  for(i=1;i<=n;i++)
   {
    printf("%d ",a[i]);
   }
 printf("\nEnter a Element location for Insertion : ");
 scanf("%d",&l);
 printf("\nEnter the Element to Insert in the Array : ");
 scanf("%d",&);

 getch();
 clrscr();
}